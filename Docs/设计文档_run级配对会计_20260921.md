# 设计文档：Trade 统计改「run 级配对会计」

- 版本：**v3.2**（v3.1 = 评审 8 条 + 复审 R1/R2；**v3.2 = 已实施**）
- 日期：2026-09-21（v3 吸收评审 15:32；v3.1 吸收复审 16:09；v3.2 实施完成 18:15）
- 作者：WorkBuddy（与用户逐例对齐后成文；v3 吸收《评审报告_run级配对会计方案_20260921》；v3.1 吸收《复审报告_v3_run级配对会计_20260921》）
- 状态：**已实施并交付**（2026-09-21 晚，用户授权后）。交付包 = `chanpy_run_accounting_2026-09-21.zip`（13 文件按仓库原目录层级覆盖：`Engine.py` / `Instrument.py` / `Reconcile.py` + 新增 `test_p52_run_accounting.py` + 9 个旧测试对齐新语义）。回归：全量 110 测试 post 失败集 == base 失败集（12 个全为环境性/既有，**零补丁新增红**）；p52 新测试 54/54、p27 63/0、p34 40/0、p26 术语守卫 24/0；交付包解压复验（md5 往返 13/13 + py_compile + 新旧标记 grep + 覆盖树冒烟）全过。
- 关联：`Trading/Engine/Engine.py`（_book_close / _run_start / _run_end）、`Trading/Infra/TradeStats.py`、`Trading/Infra/StateDB.py`（trades 表）、`Trading/Infra/Records.py`（OrderIntent / AccountState）
- 评审基线核对：26/26 代码断言一致（评审报告 §1）；对账恒等式经独立复演 5 个已收口场景三账全等（评审报告 §2）；复审补下载 SimNow.py 后断言全量命中、恒等式复演 7 场景（4 收口全等 + 3 在途部分和）

---

## 0. 一句话

把成交统计的记账口径从「**仓单级**（每笔平今/平昨成交 = 一笔交易）」改为「**run 级**（每次风控运行 = 一笔交易 = 一对成交：入场 fill + 离场 fill）」，使 R-OPEN 锁仓离场与拆锁的盈亏按信号正确归因；**总净盈亏不变**（对账恒等式见 §3.1），恢复的是胜率 / 盈亏比 / 平均盈亏 / 最大单笔的"每笔"语义。

---

## 1. 背景与问题

### 1.1 现状（代码证据，评审已逐条核实 26/26）

| 事实 | 位置 |
|---|---|
| Trade 只有两条写入路径 | `_book_close`（Engine.py:1503-1554，`save_trade`@:1546）；对账补记（Reconcile.py:361-374，reason=reconcile_external_partial） |
| `_book_close` 仅在 CLOSE 意图成交时调用 | Engine.py:1462-1465：`if act.intent is OrderIntent.OPEN: _book_open(...) else: _book_close(...)` |
| R-OPEN 锁仓离场走 **OPEN** 意图 | Records.py:125："④ 反向开仓锁仓也走它" |
| 锁仓离场成交后只做 toast + run_end，**不写 Trade** | Engine.py:1470-1473（`net_before!=0 && net_after==0` 分支） |
| 统计面板读 trades 表 | TradeStats.load_trades_report（TradeStats.py:40+，纯只读 SELECT） |

旁证（评审补）：`_execute` 的 net 变化分支**二值互斥**（`==0→!=0` 与 `!=0→==0`），不存在第三分支——这正是 §3「run 与净敞口一一对应、串行」的代码级保证。

### 1.2 后果（用户推演 + 今日实盘）

用户例子（IF，第一天 4000 开多 A → 4010 反向开空 B 锁仓；第二天 3990 平昨 A、3970 平昨 B）：
现网会记成两笔 Trade：A 仓 4000→3990（−10 点）、B 仓 4010→3970（+40 点），胜率 50%；而经济上这是"多 4000→4010（+10）+ 空 4010→3970（+30）"两笔全胜的交易。**Σ 不变，每笔归因全错。**

今日实盘已发生两笔锁仓离场（空 4442.4→4442.6 = −0.2pt、空 4439.8→4442.6 = −2.8pt），trades 表中均无记录；将来拆锁时多仓单会以 4442.6 为 entry 记账，空仓单的真实盈亏永远不会以独立 Trade 出现。

---

## 2. 口径勘误（回应讨论 ⑴）

> 前一轮"配对会计不是没设计过，是被删了"的表述**不准确，已更正**。

- `origin / lock_pair_id` 是给**三态判定**用的（"现在是不是锁着"），不是给统计用的。`Engine.py:244-246` 注释明确：曾按"持仓来源标记"判定锁仓——删除原因是"把问题绕复杂的根源"，与统计配对无关。
- 平仓选仓规则从来就是**纯 FIFO**：`PositionBook.oldest_opposite`（PositionBook.py:207-217）——"平仓就是跟持仓序列中反向最早的仓单对冲掉。簿内仓单**没有配对概念**，只有时间先后"。
- 结论：**统计层从未有过配对会计**；run 级会计也不需要恢复 origin / lock_pair_id——run 的入场价 / 离场价直接取成交回报（fill），与"拆锁时选中哪一笔仓单"无关（§6 护栏 [S3] 钉死这一点）。

---

## 3. 口径定义（SSOT，唯一权威定义）

**一笔 Trade = 一次 run = 恰好一对成交。**

| 概念 | 定义 |
|---|---|
| **入场 fill** | ① 空仓态下信号开仓成交（OPEN，开仓档费率）；或 ② 锁仓态下拆锁平仓成交（CLOSE，**平昨/平今档费率**，不是开仓档） |
| **离场 fill** | ① 今仓离场 = 反向开仓成交（R-OPEN，开仓档）；② 昨仓/两态机离场 = 平仓成交（平昨/平今档）；③ 收盘托管关闭 `shutdown_and_lock_all` 的强制锁仓成交（reason=auto_order_off；**补做重试离场 reason=auto_order_off_retry**，见 §7-④） |
| gross_points | (离场 fill 价 − 入场 fill 价) × side.sign |
| cost_cash | 入场 fill 手续费 + 离场 fill 手续费，**各按自己成交的 offset 档** |
| net_cash | gross × 有效乘数 × 手数 − cost_cash |
| exit_at / reason | 离场 fill 时点；reason = run 的出场计划与阶段（现状 `_run_plan` / `_phase` 已有）；**强制离场时 reason 取强平 reason（auto_order_off / auto_order_off_retry），不得回落到 run 计划名** |

配套不变量（已在现网成立，run 会计依赖它们）：

- **run 与净敞口一一对应、串行**：运行态不加仓（Records.py:37——信号门只放行"空仓开仓 / 锁仓拆锁"）；手数 = |net_volume|。
- run anchor 现状就是入场 fill 价（Engine.py:1468 `_run_start(o.filled_price, ...)`），拆锁 CLOSE 成交后 net≠0 同样触发 run_start——**入场侧数据已齐全，无需新增采集**。

### 3.1 对账恒等式（本设计的正确性锚）

每笔真实成交恰好归属一个 run（开仓 fill 或拆锁 fill = 某 run 的入场；反向开仓 fill 或平仓 fill = 某 run 的离场），因此：

```
Σ run.net_cash ≡ Σ 全部真实成交的现金流（含全部手续费）
```

级数裂项相消（telescoping）：前一 run 的离场 fill 价与后一 run 从对侧重新入场的价格，通过锁仓侧仓单的 entry/exit 首尾相接。**总额与仓单级严格相等，只有"切刀位置"不同。**

**成立前提（v3.1 修订，评审 §3.2 + 复审 R2）**：恒等式对**已完全平仓的成交史**成立——无在途 run，且簿内两侧仓单均为空。run 级 Trade 只在 run 结束时才存在，簿级平仓行只在平仓成交时才存在：**未全平仓时两侧同为部分和、不保证相等**——这不是设计错误，是"每笔归因只在收口时存在"的自然结果。两类未收口形态要分开看：**RUNNING 纯持仓态**两侧都还没有任何行、恰好同为 0（相等的合法快照——v3"互不相等"的反例，复审 R2）；**LOCKED 态** run 已随锁仓 fill 收口（Engine.py:1470-1473 `_run_end`）而簿内持仓未平，两侧通常不等。[S2] 护栏据此用**结构性前提断言**（簿内两侧仓单数均为 0）做门禁后再比对数值，不把"数值不等"本身当断言；§8 补产品层提示。

---

## 4. 用户例子逐笔推演（对齐验证）

记号：开 A fill=4000，止盈反向开 B fill=4010；开 C fill=c，20 点止盈反向开 D fill=c+20；第二天平昨 B fill=p₁，止盈平昨 A fill=p₁+5；平昨 D fill=p₂，止盈平昨 C fill=p₂+6。

| # | run | 入场 fill（档） | 离场 fill（档） | gross | cost | 用户记账 | 一致 |
|---|---|---|---|---|---|---|---|
| T1 | D1 信号1（空仓→多） | 开A 4000（开仓档） | 反向开B 4010（开仓档） | +10 | 开A费+开B费 | 盈利10 − 开A − 开B | ✓ |
| T2 | D1 信号2（锁仓拆锁→多） | 开C c（开仓档） | 反向开D c+20（开仓档） | +20 | 开C费+开D费 | 盈利20 − 开C − 开D | ✓ |
| T3 | D2 信号1（锁仓拆锁→多） | **平昨B p₁（平昨档）** | 平昨A p₁+5（平昨档） | +5 | 平昨B费+平昨A费 | 盈利5 − 平昨B − 平昨A | ✓ |
| T4 | D2 信号2（锁仓拆锁→多） | 平昨D p₂（平昨档） | 平昨C p₂+6（平昨档） | +6 | 平昨D费+平昨C费 | 盈利6 − 平昨D − 平昨C | ✓ |

- 拆锁选仓：空仓单 {B,D} 平最早（FIFO）= B ✓；多仓单 {A,C} 平最早 = A ✓——与 `oldest_opposite` 现网行为一致，**无需任何配对标记**。
- 恒等式抽查（真实现金流）：A 仓 (p₁+5)−4000，B 仓 4010−p₁，C 仓 (p₂+6)−c，D 仓 (c+20)−p₂；Σ = 10+20+5+6 = 41 = Σgross ✓（评审独立复演 5 个已收口场景三账全等；复审复演 4 收口全等 + 3 在途部分和）。

**结论：用户表述的口径 ≡ 本文"按 run 记账"，无分歧。** 唯一补充强调的一点：T3/T4 的入场 fill 费率是**平昨档**（用户例子已正确计入"平昨B仓手续费"）。

---

## 5. 实现草案（只列改动点，本轮不改代码）

### 5.1 改动点

1. **把"写 Trade"从 `_book_close`（仓单事件）挪到 run 结束点**：
   - 离场 = 平仓（两态机 / 昨仓）：`_book_close` 保留"移除仓单 + close 事件"，Trade 改由 run 结算写——entry 取 run 的入场 fill（**不是被平仓单自己的 entry_price**——拆锁场景下二者不同：被平的 A 仓 entry=4000 是前一天的价，run 入场是平 B 的 p₁）。
   - 离场 = 反向开仓（R-OPEN）：在 `Engine.py:1470-1473` 分支（net→0 经 OPEN）新增 run 结算写 Trade——这就是本设计要**新增**的写入点。
   - 离场 = 强制锁仓（shutdown_and_lock_all / on_bar 补做 retry）：同样走 run 结算，reason 取强平 reason。
   - **四个离场路径收敛到一个结算函数 `_settle_run(exit_price, offset, reason, at)`，防双写**（评审 P0；run 结算只在此唯一出口发生）。
2. **run 元数据扩充**（`_run_start`）：现 anchor=fill 已够价；补记 entry 成交的 **offset 档**与 entry fill 时点。**offset 取值无需新增采集**（v3，评审 §3.3）：`_execute` 作用域内已有 `act.intent`（:1462-1473），`offset = INTENT_TO_OFFSET[act.intent]` 直接可得。旁证修正（v3.1，复审 R1）：DryRun.py:70/:83 确写 `meta["offset"] = INTENT_TO_OFFSET[intent]`；**SimNow.py:1381 写的 `meta["offset"]` 实为 action 标签**（:1161/:1266 硬编码传 `open`/`close`，不含 `CLOSETODAY`，与 CTP offset 档语义不同），不可当 offset 档旁证。⚠️ 本设计一律以 `INTENT_TO_OFFSET[act.intent]` 为准、**不依赖任何 broker meta**；**禁止在 Engine 里复制第二份 INTENT_TO_OFFSET 映射**（SSOT 仍在 Broker/Base.py:84）；[S5] 对应缩小为"`_persist_run` 新增字段恢复"。
3. **Fee 计算**：cost = 单边费(入场 fill, 入场档) + 单边费(离场 fill, 离场档)。现 `state.cost_cash(entry, exit, closetoday)` 是双边语义（Instrument.py:424/:458-459），需拆为单边 ×2 或加参；**费率档 SSOT 仍在 Instrument/Fee 品种档案，不得出现第二处费率表**。
4. **`_book_close` 的仓单级 gross 事件字段保留**（events.jsonl 供对账/回放），但 trades 表不再由它写入。⚠️ **close 事件的 `trade_id` 字段必须一并处理**（v3，评审 §3.4）：现状 `ev.write("close", ..., trade_id=t.trade_id)`（:1552）——run 级后 `_book_close` 不产 Trade，落点为"**run 结算先行、拿到 trade_id 再写 close 事件**"（回填），不得留下悬空引用；[S10] 护栏断言 events.jsonl 中每个非空 `trade_id` 都能在 trades 表命中。
5. **对账补记**（Reconcile.py:361-374）：**并入 run 结算（已拍板，§7-②）**——删的是 run 对应仓单 → 以对账参考价（`ref_price` = last_bar.close or pos.entry_price，:339）对该 run 强制结算；删的是锁仓侧仓单 → 不影响 run。不再保留独立的仓单级补记行。⚠️ **结算触发点 = run 对应仓单被删除的那一刻（:337 循环体内），不是两侧循环收尾**（v3，评审 §3.5）：对账按 side 逐侧跑，两侧同时被清零时先删的可能是 run 对应仓单，延后统一结算会把时点/参考价算错；[S6] 补"两侧同时清零"用例。
6. **Frontend / main.py 日终打印 / TradeStats 公式**：零改动（继续读 trades 表，公式已被 test_trade_stats_formulas 钉住；评审已核实 compute_trade_stats 只读 net_cash）。

### 5.2 run 生命周期（改动后）

```
空仓 ──开仓 fill(开仓档)──▶ RUN(入场=fill) ──离场触发──▶
   ├─ 今仓: 反向开仓 fill(开仓档) → _settle_run 写 Trade → 锁仓
   ├─ 昨仓/两态: 平仓 fill(平昨/平今档) → _settle_run 写 Trade → 空仓
   └─ shutdown: 强制锁仓 fill → _settle_run(reason=auto_order_off[_retry]) → 锁仓
锁仓 ──信号→平昨/平今 fill──▶ RUN(入场=拆锁 fill, 平昨/平今档) ──▶ 同上
```

---

## 6. 测试计划（护栏，v3 增至 10 条；v3.1 修 [S2] 门禁）

| 编号 | 护栏 |
|---|---|
| [S1] | §4 用户四步例子端到端（喂 bar/signal）→ 断言 4 笔 Trade 的 gross / cost / 胜率 / 最大单笔；**并断言 equity_curve 末值 == total_net**（v3，评审 P2：防"跑了 run 级但曲线仍按旧口径"） |
| [S2] | **恒等式**：Σ run.net_cash ≡ Σ 全部成交现金流（含费），随机行情 property 级验证；**门禁 = 结构性前提断言**——簿内两侧仓单数均为 0（全平仓，无锁仓残留）才比对数值，**不把"两侧不等"本身当断言**（v3.1，复审 R2：RUNNING 纯持仓态两侧同为 0 是合法相等快照；v3"全平仓后才比对"口径保留） |
| [S3] | FIFO 无关性：改变拆锁选仓顺序（B↔D）不改变任何统计量（run 会计只认 fill） |
| [S4] | 费率档：拆锁入场 fill 按平昨档、R-OPEN 离场 fill 按开仓档（IF 三档费率下断言精确到分）；覆盖 TA（FAK/1 手） |
| [S5] | run 中重启：anchor / entry fill / offset 档从 state.db 恢复（`_persist_run` 新增字段），离场后 Trade 完整 |
| [S6] | 对账强平 run 对应仓单 → run 以 ref_price 强制结算，恒等式仍成立；**补"两侧同时清零"用例**（v3，评审 §3.5——run 对应仓单先删时即刻结算） |
| [S7] | shutdown_and_lock_all → Trade reason=auto_order_off，时点/费率正确 |
| [S7b] | **auto_order_off_retry 单列**（v3，评审 §3.1）：喂"托管关闭 + 首轮离场被拒 + 下一根 K 线 on_bar 重试成交"序列，断言 Trade.reason == auto_order_off_retry 且不落入 sl/tp 桶 |
| [S8] | 两态机品种（CLOSETODAY）：run 级与现仓单级逐字段一致（回归等价） |
| [S9] | **在途 run 零写入**（v3，评审 §3.6）：开仓 fill + 若干 bar 不触发离场 → 断言 trades 表笔数不变且 open_position 非空；再触发离场 → 断言恰好新增 1 笔（一次咬住"中途误写"与"离场漏写"两个方向） |
| [S10] | **events 无悬空 trade_id**（v3，评审 §3.4）：events.jsonl 中每个非空 trade_id 都能在 trades 表命中 |

---

## 7. 口径拍板（2026-09-21，4/4 已定，无遗留待确认项）

1. **旧 trades 数据 → 用户已物理删除（2026-09-21 16:09 拍板并自行执行）**。实施时**零清理、零兼容步骤**：trades 表直接按 run 级 schema 使用。评审与复审曾建议沿 `trades_legacy_points` 先例改名留存作审计底稿（StateDB.py:100 先例属实），你已裁定物理删除——此项闭环，实施层无动作。
2. **对账补记 → 并入 run 结算，不保留独立仓单级补记行**。对账删仓即 run 强制结算，trades 表只有一种行。
3. **FAK 无需任何假设 —— 已被硬断言钉死**。`Product.py:232-236`：ExecPolicy 构造期断言 `FAK ⟹ lots_per_order == 1`（"只有 N=1 时 FAK 才等价于 FOK"）；EXEC_POLICY 表中唯一 FAK 品种 TA 就是 1 手（:249）。即本系统内 **FAK ≡ FOK 是不变量而非假设**：一笔委托要么全成要么全撤，不存在部分成交，run 会计无需为此设计任何分支。[S4] 费率档测试覆盖 TA（FAK/1 手）即可。
4. **`auto_order_off` 的 reason 单列（已拍板；落点 = 数据层）**。
   - 勘误（15:09）："统计面板有一张按出场原因拆分的表"**不准确——面板没有**。2026-09-19 已拍板把「按出场原因」从面板删除（`Test/test_stats_panel_labels.py` ④，**注意路径在仓库根 `Test/`，非 `Trading/Test/`**——v3 补全，评审 §1.5），双层护栏锁死"即使后端发 by_reason 面板也不显示"（静态 :185 + 真渲染喂脏数据 :113）。
   - by_reason 数据实际在两处后端：**main.py 收尾控制台打印**（main.py:343-346，一行文本 `按出场: tp: n=… net=…  sl: …`）与 **API 报告字段**（TradeStats.load_trades_report:258 / Engine.summary:2193-2207，前端不读）。
   - **落点**：强平 Trade 的 reason 写 `auto_order_off` / `auto_order_off_retry`（trade 行 / events / 收尾打印 / 报告字段自动单列），不碰面板。sl/tp/time/trailing 四个策略 reason 语义保持纯净。
   - **双 reason（v3，评审 §3.1）**：全树强平 reason 有**两个**——`auto_order_off`（主动关闭托管，Engine.py:1771）与 **`auto_order_off_retry`**（托管已关仍有 RUNNING 残留 → on_bar 每根 K 线补平，Engine.py:774；:1592 是其 toast 分支）。两者都会经 `_force_exit → _execute` 走 run 结算；只单列前者则 retry 笔会漏进 sl/tp 桶，正是本条要防的污染。[S7b] 钉住。
   - 将来若要让"按出场原因"回到面板，属独立产品决策，需先解除 test_stats_panel_labels ④ 的两道护栏。

---

## 8. 坑点备忘

- 拆锁场景下 `_book_close` 的 `pos.entry_price` **不能**再当 Trade 的 entry 用（这正是仓单级口径错误的根源）；run 级 entry 一律取 `_run_start` 的 anchor。
- run 结算必须只在 run 结束点写**一次**：锁仓离场 / 平仓 / 对账强平 / shutdown（含 retry）路径全部收敛到 `_settle_run` 唯一出口，防双写（[S9]+[S7b] 钉住）。
- `by_reason` / equity_curve 的时点语义改为离场 fill 时点——与现状 `now_cn()` 一致；[S1] 断言曲线末值 == total_net。
- 三态判定 / 选仓 / 报单路径**零改动**——本设计只动"记账"，不动"执行"（一进一出原则不受影响）。
- FAK ≡ FOK 是 ExecPolicy 构造期硬断言（Product.py:232-236）——未来若有人放开"FAK + N>1"，既有断言会先拦下；run 会计无需防御性代码。
- **未全平仓的部分和**（v3.1，复审 R2）：RUNNING/LOCKED 态下 Σrun 与 Σ仓单级同为部分和、**不保证相等**（RUNNING 纯持仓态两侧同为 0 是合法相等快照；LOCKED 态 run 已收口而簿内持仓未平，通常不等）；产品层提示——统计面板"总净盈亏"在锁仓态下**天然不含未兑现持仓**，与现状一致（现状同样不计），不是回归；恒等式只在全平仓后成立（[S2] 结构性门禁）。
- **两 broker 的 `meta["offset"]` 语义不等价**（v3.1，复审 R1）：DryRun 写 `INTENT_TO_OFFSET[intent]`（OPEN/CLOSE/CLOSETODAY），SimNow 写 action 标签（open/close）——任何"读 broker meta 选费率档"的新代码都会在 SimNow 路径静默取错档；本设计主路径 `INTENT_TO_OFFSET[act.intent]` 天然规避。

---

## 9. 评审意见处理表（v3）

来源：《评审报告_run级配对会计方案_20260921》（基线 b4880c6fdb，26/26 断言机器比对 + Fraction 独立复演）。逐条核实后 **8 条全部接纳、0 条拒绝**（第 3 条附实施偏好修正）；原"待确认"的清库物理形态已由你拍板——**物理删除**（§7-①）。复审新增意见见 §9.1。

| # | 评审条目 | 级别 | 处理 | 理由（核实证据） |
|---|---|---|---|---|
| 1 | §3.1 漏 `auto_order_off_retry` | P1 | **接纳** | 全树核实：Engine.py:774 `_force_exit(bar, reason="auto_order_off_retry")` 存在（on_bar 补做路径），:1771 为主动关闭路径，:1592 有其专属 toast 分支——强平 reason 确有两个，只列一个会漏。§3/§7-④/[S7b] 已补 |
| 2 | §3.2 恒等式需限定"已收口" | P0 | **接纳** | 逻辑必然：run 级 Trade 只在 run 结束存在，在途 run 的入场 fill 现金流与手续费无行承载，部分和必不等。§3.1 已补前提；[S2] 改为全平仓后比对；§8 补面板提示（v3.1 按复审 R2 进一步校准措辞） |
| 3 | §3.3 offset 无需新增采集 | 建议 | **接纳（附实施偏好）** | 核实 DryRun.py:83、SimNow.py:1381 均写 `meta["offset"]`；但交易引擎侧更倾向直接 `INTENT_TO_OFFSET[act.intent]`（`_execute` 作用域内现成），不依赖 broker meta 约定。两者同一 SSOT 表，禁止复制第二份映射——采纳其 SSOT 警告（v3.1 按复审 R1 修正 SimNow 旁证：其 meta["offset"] 实为 action 标签，语义不等价） |
| 4 | §3.4 close 事件悬空 trade_id | P1 | **接纳** | 自核 Engine.py:1552 `trade_id=t.trade_id` 确在 close 事件里；`_book_close` 不再产 Trade 后必须回填或删除。落点选"结算先行回填"；[S10] 已补 |
| 5 | §3.5 对账结算触发时点 | P1 | **接纳** | 核实 Reconcile.py:337-386：删仓在逐侧循环体内发生；两侧同时清零时 run 对应仓单可能先删，延后结算会错时点/参考价。[S6] 补用例 |
| 6 | §3.6 缺"在途 run 零写入"护栏 | 建议 | **接纳** | 双写是最可能的回归形态，[S9] 一条同时咬住"中途误写"与"离场漏写" |
| 7 | §1.5 路径疏漏（Test/ 在仓库根） | 笔误 | **接纳** | 自核：test_stats_panel_labels.py 确在仓库根 `Test/`，文档已补全路径 |
| 8 | §4-P2 清库改"改名留存" | P2 | **已拍板：物理删除** | 核实 StateDB.py:95-100 有 `trades_legacy_points` 改名先例，评审建议采纳；你于 16:09 拍板**已物理删除**旧 trades 数据（§7-①）——此项闭环，实施层零清理动作 |
| — | 其余：26 条断言核对、恒等式 5 场景复演、§0 verdict | — | **无异议** | 抽样自核全部命中；评审方法（机器比对 + Fraction 复刻 + 附录 B 自我纠错记录）可信 |

### 9.1 复审处理表（v3.1）

来源：《复审报告_v3_run级配对会计_20260921》（落文核对 8/8 + 补下载 SimNow.py 后断言全量核对 + Fraction 复演 4 收口/3 在途场景）。

| # | 复审条目 | 级别 | 处理 | 理由（核实证据） |
|---|---|---|---|---|
| R1 | §5.1-2/§9-3 引 `SimNow.py:1381` 当 offset 档旁证**失实** | P1 | **接纳（按其选项 A）** | 自核属实：:1381 写 `"offset": action`；action 由 :1161/:1266 硬编码 `open`/`close` 传入（:1279 签名 `intent_str` 与 `action` 分离，intent.value 走第 2 参），取值域 {open, close} **永不含 CLOSETODAY**；DryRun.py:70/:83 才是 `INTENT_TO_OFFSET[intent]`。已删 SimNow 旁证、§5.1-2/§8 明示两 broker meta 语义不等价；主路径 `INTENT_TO_OFFSET[act.intent]` 不变（复审亦确认不阻塞实施）。**附修正**：复审称 `meta["offset"]` 读者仅 test_p30 三处——实测 **3 文件 7 处**（test_p30:192/245/306、test_p34:198/229/281、test_p51:341），但全部构造 DryRunBroker（test_p30:163 / test_p34:157 / test_p51:337），"SimNow 侧零测试覆盖"的结论不变；test_p51:341 断言 `CLOSETODAY` 恰好反证大小写分歧只在 DryRun 侧被测到 |
| R2 | §3.1 "互不相等"措辞过强 | P2 | **接纳** | 反例核实成立：RUNNING 纯持仓态下 run 未收口、簿未平仓，两侧均零行、恰好相等（v3 原文"互不相等"被此反例证伪）；LOCKED 态 run 已随锁仓 fill 收口（Engine.py:1470-1473 `_run_end`）而簿内持仓未平、通常不等。§3.1 改"不保证相等"+ 区分两类形态；[S2] 改**结构性前提断言**（簿内仓单数为 0 门禁），不把数值不等当断言手段 |
| R3 | 复审 §6 建议清库走"改名留存" | — | **不采纳（已由你拍板）** | 你于 16:09 拍板**已物理删除**旧 trades 数据（§7-①）——实施层零清理动作，此项闭环。改名留存的"审计底稿"价值已记录在案；物理删除后如需对照，只能依赖 events.jsonl / 外部备份 |
| — | 其余：落文核对 8/8、断言全量命中（补 SimNow.py 后 100%）、计数口径确认（6 修订 + 1 笔误 + 2 P2 归并为 8 行成立）、方法附录 | — | **无异议** | 抽样自核命中（Engine.py:774/:1592/:1552、Reconcile.py:339、StateDB.py:100、Base.py:84、DryRun.py:83 等与复审一致）；其"跨函数追踪定性 :1381"的做法正确 |

**净结论：方案方向维持不变；v3.1 已吸收评审 8 条 + 复审 R1/R2，拒绝项仅复审的"改名留存"建议（与你已执行的物理删除拍板冲突）；全部口径已定、零待确认项，待你授权实施。**
