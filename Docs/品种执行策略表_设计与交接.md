# 品种执行策略表（Trading/Infra/Product.py）· 设计与交接文档

> **角色**：交接文档（handoff）。让接手的人**不考古就能继续**。
> **状态**：🟢 **已实现 + 全量回归通过 + 已打 zip 交付**（详见 §14）。
> **日期**：2026-09-16
> **基线**：远端 `custom-dev` HEAD = `2701b6b1c8bf`（2026-09-15T12:15:53Z）；本地 `C:\my_chan_project` 与远端 5 个关键文件 sha 完全一致（Product / Engine / Instrument / SimNow / Base）。
> **实现**：沙盒 `C:\Users\river\WorkBuddy\2026-09-16-05-21-13\sandbox\chanpy\`（**未直接改 `C:\my_chan_project`**）；改动 19 个 `.py`，全量测试 **52/52 绿**；交付 zip = `deliver\chanpy_exec_policy_2026-09-16.zip`（整文件覆盖语义，无 patch/diff，`.md` 不进包）。
> **参考文档**：`Docs/账户三态（空仓-运行-锁仓）.html`（三态 + 六条转移 + Phase 10 D6 平今开关附注）。

---

## 0. 一页速览

| 项 | 结论 |
|---|---|
| 要做什么 | 在 `Trading/Infra/Product.py` 建一张 **8 行 × 5 列**的「品种执行策略表」，把散落三处的判断（平今 / FOK-FAK / 单笔手数）收成一张表 |
| 行为变更 | **零**（8 品种 × 5 列与当前代码派生值 8/8 一致），唯一可见变更是启动横幅文案 |
| 代码判据 | **只看品种 + 表的第 3/4/5 列**。禁止任何按交易所名字判断的逻辑 |
| 第 2 列交易所 | **纯注释**，不参与任何判断，也不做对账告警 |
| 覆盖档费率 | 决策侧**完全不读**；数据保留给会计侧 `cost_cash` |
| 落地形态 | 模块级 `EXEC_POLICY: Dict[str, ExecPolicy]` + `_exec_kw(code)` 注入（与现有 `_fee_kw` 同构） |
| 硬不变式 | `FAK ⟹ N == 1`（构造期硬断言 + 守护测试） |
| 待办（0 条） | 全部拍板完毕。§9 定为 **D：消除 Optional、两路平铺**（不是选兜底）；注释批 2 本次一起做 |
| 已拍板 | 注释批 2（纯考古型约 30 处）**本次一起做** |
| **实现状态** | ✅ **已完成**：改动 19 个 `.py`（生产 7 + 测试 12），全量回归 52/52 绿，zip 已交付并解压复验 |

---

## 1. 背景与目标

### 1.1 现状的三处散落判据

当前「今仓怎么离场 / 用什么报单属性 / 一笔挂几手」分散在三处，且其中一处要靠**费率计算**：

| 判据 | 现状来源 | 位置 |
|---|---|---|
| 走不走平今 | `supports_closetoday`（exchange ∈ SHFE/INE）**且** `prefer_closetoday`（费率 3× 口径） | `Product.py:266-319`、`Instrument.py:323-341`、`Engine.py:1018-1019` |
| FOK / FAK | `if exchange == "CZCE"` | `Instrument.py:357-380` |
| 单笔手数 N | `if exchange == "CZCE"` → 钉 1；否则 `lots_per_signal` | `Engine.py:947-962` |

### 1.2 用户提出的三个动机

1. **去掉费率派生**：让代码按交易费率算"平今是否划算"太复杂（AU/AG 还有覆盖档两种费率）。改由用户**提前算好、填进表**，代码只读表。
2. **FOK/FAK 集中化**：原先散在代码里，收进同一张表。
3. **N 可按品种配**：原先所有品种共用 `lots_per_signal`，改为每个品种可有自己的 N。

---

## 2. 需求原文存档（四轮，按时间序）

### 第 1 轮（用户初始需求）

> 在 `Infra/Product.py` 中建一张表，目前支持的 8 个期货品种，每个占一行：
>
> | 品种 | 所属交易所 | 是否支持平今指令 | 报单方式 | 1 笔挂 N 手 |
> |---|---|---|---|---|
> | IF | CFFEX | CLOSE | FOK | 2 |
> | IH | CFFEX | CLOSE | FOK | 2 |
> | IC | CFFEX | CLOSE | FOK | 2 |
> | IM | CFFEX | CLOSE | FOK | 2 |
> | AU | SHFE | CLOSETODAY | FOK | 2 |
> | AG | SHFE | CLOSETODAY | FOK | 2 |
> | CU | SHFE | CLOSETODAY | FOK | 2 |
> | TA | CZCE | CLOSE | FAK | 1 |
>
> 第三列为 `CLOSETODAY` 表示支持平今指令；否则不支持。
>
> - 第 3/4/5 列 = `CLOSE FOK N`：遵循「账户三态」状态机，按 1 笔挂 N 手报单。
> - 第 3/4/5 列 = `CLOSE FAK 1`：同样遵循状态机，只是按 1 笔挂 1 手报单。
> - 第 3/4/5 列 = `CLOSETODAY FOK N`：**简化版状态机，仅两态**（空仓态 / 运行态）。
>   空仓态有信号就 OPEN 开仓；运行态依赖止盈止损 L1-L3 执行 CLOSETODAY 平今。
>
> 为何这样改：用户已按费率算好是否平今，代码无需再算；FOK/FAK 集中化；N 扩展为按品种可配。

### 第 2 轮（用户四点修正）

> ⑴ 第二列交易所在代码中**无需任何判断**，完全看第三列。上期所虽支持平今，但不是每个品种平今费率都便宜，只有我算出平今便宜的才写 `CLOSETODAY`。
> ⑵ `FAK ⟹ N == 1` 用守护测试限制住即可（表格里 FAK 的行最后一列必须为 1）。
> ⑶ 承上：做**硬断言（构造期拒绝启动）**，不做软警告。
> ⑷ CU 我写错了，改为 `CLOSE`。同样是上期所品种、支持平今指令，但有些品种费率不适合平今。

### 第 3 轮（用户三点核对）

> ⑴（回应我的 CU 复算）**无需代码考虑覆盖档问题，这就是我要的简化**：用户会在交易特定品种特定月份合约前，自己按费率算出是否平今便宜，便宜就去改表格配置，然后才启动程序。
> ⑵ **代码逻辑中绝对不要出现拿交易所名字判断的代码逻辑**，一定注意，只看品种，品种的第三四五列才决定代码逻辑走向。
> ⑶ `CLOSETODAY ⟹ 交易所 ∈ {SHFE, INE}` 这条断言 —— **删**，第 2 列就是纯注释。一定要确保"纯重构，零行为变更"。

### 第 4 轮（用户四点确认）

> ⑴ 覆盖档：代码无需考虑（见第 3 轮 ⑴）。
> ⑵ 待定 3 处（比的是交易所名字但不决定走向）—— **删**。第 2 列是纯注释。
> ⑶ `Engine.py:1023` 的 `if today_target is not None:` 兜底 —— 还有用吗？没有用就删吧？
> ⑷ 按最新设计落地时，**顺手把代码中跟表格设计不相符的注释全部清理掉**，放在那里没用而且容易误导。

### 第 5 轮（用户追问兜底）

> 我还是不清楚那个兜底为何会走到。按我理解，如果走到平今流程，平完持仓就是空仓态。这个流程不会出现 CLOSE 那种"平仓后再次进入锁仓态"（后者跨日有可能好几锁，A-B 互锁、C-D 互锁；但平今流程都是 开→平→空仓→再开→再平→空仓）。

---

## 3. 最终定表

| 品种 | 交易所（纯注释） | 今仓离场 offset | 报单属性 | 1 笔挂 N 手 |
|---|---|---|---|---|
| IF | CFFEX | `CLOSE` | `FOK` | 2 |
| IH | CFFEX | `CLOSE` | `FOK` | 2 |
| IC | CFFEX | `CLOSE` | `FOK` | 2 |
| IM | CFFEX | `CLOSE` | `FOK` | 2 |
| AU | SHFE | `CLOSETODAY` | `FOK` | 2 |
| AG | SHFE | `CLOSETODAY` | `FOK` | 2 |
| **CU** | SHFE | `CLOSETODAY` | `FOK` | 2 |
| TA | CZCE | `CLOSE` | `FAK` | 1 |

> ⚠️ **CU 的曲折**：第 2 轮用户口误写 `CLOSE` → 第 3 轮我按现有 3× 口径复算后**翻回 `CLOSETODAY`**（见 §4.2），用户确认。以本表为准。

---

## 4. 零行为变更论证（8/8 逐格对照）

### 4.1 当前代码派生值 vs 表格

| 列 | 当前派生链 | 表格值 | 一致 |
|---|---|---|---|
| 今仓离场 | `supports_closetoday`(exchange∈SHFE/INE) **且** `prefer_closetoday`(平今费 < 3× 开仓费) | IF/IH/IC/IM、TA → `CLOSE`；AU/AG/CU → `CLOSETODAY` | ✅ |
| 报单属性 | `Instrument.effective_order_advanced()`：CZCE → `FAK`，其余 → 配置（默认 `FOK`） | TA → `FAK`；其余 → `FOK` | ✅ |
| 单笔手数 | `Engine._open_volume()`：CZCE → 1；其余 → `lots_per_signal`(= `risk.max_volume`，默认 2) | TA → 1；其余 → 2 | ✅ |

**⇒ 8 品种 × 3 个决定列 = 24/24 一致 ⇒ 纯重构，零行为变更。**

### 4.2 CU 复算（3× 口径，逐笔）

路径定义见 `Product.py:219-232`：平今路径 = 2 笔（开 A + 平今 A）= X + Y；锁仓路径 = 4 笔（开 A + 反向开 B + 次日平 A + 次日平 B）= 4X。**判据：Y < 3X → 平今。**

| 品种 | X（开仓） | Y（平今） | 平今 X+Y | 锁仓 4X | 结论 | 省 |
|---|---|---|---|---|---|---|
| AU | 10 元/手 | 免收 | 10 元 | 40 元 | 平今 | 75% |
| AG | 万 0.1 | 万 0.1 | 万 0.2 | 万 0.4 | 平今 | 50% |
| **CU** | 万 0.5 | 万 1.0 | 万 1.5 | 万 2.0 | **平今** | 25% |
| IF/IH/IC/IM | 万 0.23 | 万 2.3 | 万 2.53 | 万 0.92 | 锁仓 | 平今贵 175% |
| TA | 3 元/手 | 免收 | 3 元 | 12 元 | 费率算平今，**但表填 CLOSE** | 75% |

- CU 按 8 万元/吨估算：开仓 20 元/手、平今 40 元/手 → 平今 60 元 vs 锁仓 80 元，**省 20 元/手**。
- CU **无覆盖档**（`OVERRIDES` 只有 AU/AG），结论不随合约月份翻转。
- ⚠️ **TA 是本设计的"活证据"**：费率算平今省 75%，但 CZCE 没有平今指令 → 表填 `CLOSE`。
  **这正是"代码只能读表、不能自己按费率算"的最好理由。**

---

## 5. 代码落地设计

### 5.1 落地形态：一张真表 + `_exec_kw()` 注入

```python
# Infra/Product.py（模块级）
@dataclass(frozen=True)
class ExecPolicy:
    today_exit: str      # "CLOSE" | "CLOSETODAY"  ← 表第 3 列
    advanced: str        # "FOK"   | "FAK"          ← 表第 4 列
    lots_per_order: int  # N ≥ 1                    ← 表第 5 列

EXEC_POLICY: Dict[str, ExecPolicy] = {
    "IF": ExecPolicy("CLOSE",       "FOK", 2),
    ...
    "TA": ExecPolicy("CLOSE",       "FAK", 1),
}

def _exec_kw(code: str) -> dict: ...
```

- **与现有 `_fee_kw(code)`（D-B 生成式 SSOT）完全同构** —— 不引入新概念。
- **不往 `Infra/` 加文件**（该目录被交接文档 §5.1 定死 7 个模块）。
- **缺行 = 启动期 KeyError**，不留静默默认值。
- `PRODUCT_PROFILES` 条目保留：策略标定（`r_multiple_tp`）+ 合约事实（`price_tick` / `multiplier`）+ 费率（生成）。

### 5.2 不变式与护栏

| 编号 | 不变式 | 强度 | 理由 |
|---|---|---|---|
| **I1** | `FAK ⟹ N == 1` | **构造期硬断言（拒绝启动）+ 守护测试** | 表格化**新引入**的最大风险。CZCE 现在敢用 FAK，靠的是"1 手没有剩余可撤"→ FAK ≡ FOK。一旦放开 `FAK + N=5`，会产生**部分成交**，直接破坏「报单填充三态（待报/全成/全撤）」不变量 4 |
| ~~I2~~ | ~~`CLOSETODAY ⟹ 表格第 3 列 == CLOSETODAY`~~ | 保留（改写判据，见 §7.3） | |
| ~~I3~~ | ~~`CLOSETODAY ⟹ exchange ∈ {SHFE, INE}`~~ | **已删除**（用户第 3 轮 ⑶） | 第 2 列是纯注释，填错由人负责 |

### 5.3 必须顺手修：手数三源不一致（**这是真 bug**）

| 位置 | 现在用什么 | 问题 |
|---|---|---|
| `_open_volume()` `Engine.py:947-962` | N（表格第 5 列） | — |
| 转移③ `Engine.py:994` | `lots_per_signal` | 与 N 脱钩 |
| 转移⑤ `Engine.py:1035` | `abs(net)` | 与 N 脱钩 |

- 现状 TA 靠"OPEN 钉 1 手 → 持仓恒为 1 → `min(2,1)=1`"**侥幸正确**。
- **一旦把 TA 的 N 从 1 放开成任意值，转移③ 只会按旧 `lots_per_signal=2` 拆，剩下的手数悬空。**
- **统一为 `min(lots_per_order, target.volume)`**。不改这条，"N 可按品种配"就是半个功能。

---

## 6. 必删清单（代码 + 判据）

| # | 位置 | 现在干什么 | 处置 |
|---|---|---|---|
| 1 | `Instrument.py:373` | `if exchange == "CZCE" → FAK` | 删 → 读表第 4 列 |
| 2 | `Engine.py:960` | `if exchange == "CZCE" → 钉 1 手` | 删 → 读表第 5 列 |
| 3 | `Engine.py:1018` | 转移④ 的 `supports_closetoday` 闸门 | 删 → 读表第 3 列 |
| 4 | `Engine.py:1038-1063` | `_prefer_closetoday()` 整个方法 | 删 |
| 5 | `Product.py:266-276` | `supports_closetoday`（`in ("SHFE","INE")`） | 删 |
| 6 | `Instrument.py:331` | 同名转发属性 | 删 |
| 7 | `Product.py:290-319` | `prefer_closetoday` / `prefer_closetoday_at` | 删 |
| 8 | `Product.py:219-232` | `LOCK_PATH_MULT` + 3× 口径注释 | 删 |
| 9 | `Product.py:237-260` | `__post_init__` 静态派生 | 删 |
| 10 | `Instrument.py:572-584` | `derive_exchange()` | 删（连带 `SimNow.py:83` import、`main.py:51` import） |
| 11 | `main.py:225-229` | 离线对账 `derive_exchange(symbol) != instr.exchange` | 删 |
| 12 | `SimNow.py:797-808` | 在线对账 → `exchange_mismatch` 告警 | 删（告警码消失） |

删完 `exchange` 只剩**两处纯展示**消费：`Instrument.describe()`（`Instrument.py:353`）、启动横幅（`main.py:98`）。**字段本身保留**（第 2 列是纯注释，字段是它的载体）。

---

## 7. 必改清单（判据换源，不是删）

| # | 位置 | 现在 | 改成 |
|---|---|---|---|
| 1 | `Engine.py:1214-1216` | `CLOSETODAY and not supports_closetoday` → `closetoday_not_supported` | 判据换为「该品种表第 3 列 == `CLOSETODAY`」。**防的是未来有人绕过 `_decide_exit` 直接构造动作**，不是交易所判断 |
| 2 | `main.py:78-96` | 启动横幅打印费率派生结论 | 打印表第 3/4/5 列（**唯一可见行为变更**） |
| 3 | `Product.py:207` | 字段注释「平今能力闸门 / FOK-FAK 语义的档案侧来源」 | 改成「纯注释，不参与任何判断」 |
| 4 | `Product.py:209-217` | 覆盖档注释里「决策侧不受影响（AU 覆盖档仍平今免…）」 | 改成「覆盖档仅供会计侧 `cost_cash`；决策侧不读费率」 |
| 5 | `Product.py:278-288` | `fee_pair` docstring 里「`prefer_closetoday` 仍只读基准档」 | 改写 |
| 6 | `Engine.py:994` / `:1035` | 转移③⑤ 手数 | `min(lots_per_order, target.volume)`（见 §5.3） |

---

## 8. 注释清理

### 批 1（本次一起做 —— 不清理就是**错误文档**）

| 文件 | 行 | 内容 |
|---|---|---|
| `Engine.py` | 938-946 | A1/A3 架构注释里的「Q2 平今开关」 |
| `Engine.py` | 947-959 | `_open_volume` docstring 整段（"CZCE 钉 1…"）→ 重写为"读表第 5 列" |
| `Engine.py` | 969-971 | 转移① 注释里的「CZCE 钉 1」 |
| `Engine.py` | 1008-1017 | P-A 平今派生长注释 10 行 → 重写 |
| `Engine.py` | 1038-1063 | `_prefer_closetoday` 方法 + docstring → 随方法删 |
| `Engine.py` | 1211-1213 | 「CLOSETODAY 仅 SHFE/INE」→ 换判据 |
| `Product.py` | 207 / 210-216 / 219-232 / 237-260 / 266-276 / 278-288 / 290-319 | 见 §6 §7 |
| `Instrument.py` | 109-110 | 「FOK/FAK 按交易所」 |
| `Instrument.py` | 276 / 323-341 / 357-380 / 572-584 | `supports_closetoday` / `effective_order_advanced` / `derive_exchange` |
| `SimNow.py` | 38 / 797-808 / 962-963 / 1158-1162 | 「郑商所不支持 FOK」；「系统永远不需要 CLOSETODAY」（**已过时**）；P-A 派生 |
| `Config.py` | 507 | 「全部报单都是 FOK」 |
| `Broker/Base.py` | 68-86 | 「CLOSETODAY 仅 SHFE/INE」→ 改为「由品种执行策略表第 3 列决定，本表只做 tqsdk 白名单」 |
| `Records.py` | 65 / 82-83 / 93 | 同样的 SHFE/INE 说明 |
| `main.py` | 78-96 / 220-229 | 费率横幅 / exchange 对账 |

### 批 2（**已拍板：本次一起做** —— 用户 2026-09-16 06:31）

纯考古型（描述"曾经有什么"，与表格设计无关），约 30 处：
`Engine.py:1077-1083`、`main.py:69 / 122 / 143`、`Config.py:20-21 / 65 / 71 / 166`、`SimNow.py:457 / 714 / 863` 等「P-A / P-B（2026-09-15）…」注释。

**理由**：它们描述的是"曾经有过什么"，对新读者是噪音，留着容易误导。与批 1 合并后一次清干净。
⚠️ **合并的代价**：diff 变大，"零行为变更"的验收面被稀释。
   **补偿措施**：批 1 + 批 2 拆成两次提交（第一次只动判据与代码，第二次只动纯考古注释），
   两次都跑全量测试，第二次必须**零测试变更**。

### 明确**不动**的注释

描述**当前不变量 / 契约 / why** 的：
- `Engine.py:1221-1243`（CLOSE/CLOSETODAY 目标日期必须匹配、手数上下界）
- 不变量编号、契约测试 p35 / p51 引用
- `SimNow.py` 成交校验的 why（平今/平昨仓位判据）
- `Clock.py:76 / 116` 规则说明
- `Config.py:378 / 384 / 437`（中金所限价单单笔上限 20 手 —— 是风控上限说明，不是品种判断）

⚠️ `Engine.py:1223-1224` 里「中金所没有平今指令…按平今费率收费 0.0345%（15 倍）」是**为什么**的说明、**不是判断**；按用户第 3 轮 ⑵ 的口径（禁止的是判断逻辑，不是说明）保留。

---

## 9. `Engine.py:1023` 分支的处置（**已闭环 · 结论 D：消除 Optional，不做兜底**）

> **最终结论（第 7 轮拍板）**：不是"选 A/B/C 哪个兜底"，而是**根本不该有兜底**。
> 前几轮我误把末尾的「反向 OPEN 锁仓」当成了 `today_target is None` 的降级分支，
> 实际上它是 **CLOSE 品种（IF/IH/IC/IM/TA）的正常出口**。`if today_target is not None:`
> 存在的唯一理由是 Python 里 `None.volume` 会抛 `AttributeError`（安抚类型检查器），
> **不是因为领域里存在第二条路**。故处置 = **删掉这个 Optional 查找，两路平铺**。

### 9.1 代码现状

```python
# Engine.py:1018-1028（_decide_exit 转移④）
if (self.state.supports_closetoday
        and self._prefer_closetoday(float(latest.entry_price))):
    today_target = _oldest([p for p in self.positions.positions
                            if p.side is net_side and p.entry_date >= today])
    if today_target is not None:                       # ← 纯类型护栏，非领域分支
        return _Action(OrderIntent.CLOSETODAY, net_side,
                       min(abs(net), today_target.volume),
                       today_target, is_exit=True, transition=4)
return _Action(OrderIntent.OPEN, _opposite(net_side), abs(net),
               None, is_exit=True, transition=4)        # ← 【不是兜底】CLOSE 品种的正常出口
```

**⚠️ 结构要点**（这是前几轮判断失误的根源）：

最后那句 `return ... OPEN` **不在任何 `else` 里** —— 它是外层 `if` 之后的**直行语句**，
因此有**两条完全不同的路径**到达它：

| 到达路径 | 性质 | 频率 |
|---|---|---|
| 外层 `if` 判否（第 3 列 = `CLOSE` 的品种） | **正常主路径**，IF/IH/IC/IM/TA 每天都在走 | 极高 |
| `today_target is None` 掉下来 | **结构性不可达**（见 §9.4） | 0 |

把这两条混为一谈，才会得出"平今流程需要一个降级兜底"的错误结论。

### 9.2 两态状态机（**修正版** —— 我上一轮理解错了）

```
                ① 有信号 → OPEN N 手
    ┌──────────────────────────────────────►┐
    │                                        │
 空仓态                                   运行态
 net = 0 · 无仓单                      net ≠ 0 · 仅一笔仓单
    │                                        │
    └──────────────────────────────────────┘
                ② 离场触发 → 平 N 手

 ② 的出口只看一件事：当前持仓是今仓还是昨仓（entry_date vs 今日）
   · 今仓（entry_date ≥ today）      → CLOSETODAY 平今 N 手 → 空仓态
   · 昨仓（entry_date < today，跨夜遗留）→ CLOSE 平昨 N 手   → 空仓态
   ⇒ 两种出口都直接回空仓，锁仓态不存在

 运行态收到新信号 → 忽略（规则 ③）；锁仓态 ⇢ 不可达
```

**要点**：用不用 `CLOSETODAY` **只取决于当前持仓是不是今仓**。昨仓毫无疑问走 `CLOSE`（平昨）。
两态 = 空仓态 ⇄ 运行态；A-B 互锁 / C-D 互锁是 `CLOSE` 品种（三态）的专利。

### 9.3 我上一轮错在哪（三条，都已修正）

1. **把"跨夜遗留"错当成 `today_target` 缺失的来源** —— 实际上昨仓走**转移⑤（CLOSE 平昨）**，
   压根不进转移④，平完直接回空仓。它不会让 `today_target` 变 `None`。
2. **其余"外力场景"也站不住**：持久化恢复混排、外部终端手工开仓 —— 引擎的 `positions` 簿
   **由引擎自己维护**，只有转移表能改它，而转移表在 `CLOSETODAY` 品种上只能产出「空仓 / 单笔运行」两态。
3. **一处技术性错误**：我上一轮说"删掉后撞 `_pre_trade_check:1217` 拒单"——**错**。
   删掉 `if` 之后紧接着就是 `min(abs(net), today_target.volume)`；若 `today_target is None`
   会抛 **`AttributeError` 崩溃**，根本走不到 `_pre_trade_check`。

### 9.4 结论：`today_target is None` 是**结构性不可达**（不是"概率极低"）

进入转移④ 的条件是 `net != 0` **且** `latest.entry_date >= today`。在 `CLOSETODAY` 品种上，
运行态**有且只有一笔仓单**（只能由转移① 产生、平完即空仓），于是
`net_side` ≡ 那笔的方向、`latest` ≡ 那笔、`today_target` ≡ 那笔 ⇒ **`None` 不可能**。

### 9.5 我错在哪（第四条 · 最根本的一条，第 7 轮补）

**把「反向 OPEN 锁仓」当成了兜底降级分支。** 它是外层 `if` 之后的直行语句，
是 `第 3 列 = CLOSE` 品种（IF/IH/IC/IM/TA）的**正常出口**，不是为 `today_target is None` 准备的逃生门。
于是我误以为"平今流程需要一个降级兜底"，进而提出 A/B/C 三个"兜底风味"让用户选 ——
**问题从一开始就是伪命题**。用户第 7 轮的反驳（"都是正常的状态机在运转"）成立。

前三条错误见 §9.3；它们都是在这条根本错误的框架下做的细节修补。

### 9.6 结论 D：消除 Optional，两路平铺（**最终方案**）

不做 A（保留降级）、不做 B（裸删 → `AttributeError`）、不做 C（写事件 + return None）。
取而代之：**让 `today_target` 这个 `Optional` 根本不产生** —— 直接用 `latest`。

`latest` 在进入转移④ 时**已由 `:1002-1004` 保证非 None**，且在两态机上**就是那笔唯一的当日仓**，
所以它与 `today_target` 是同一个对象，无需再查一次。

```python
# 重构后（_decide_exit 转移④）
if latest.entry_date >= today:
    # 转移 ④：今日仓离场。口径唯一来源 = 品种执行策略表第 3 列。
    if policy.close_mode is CloseMode.CLOSETODAY:
        # 两态机：运行态有且只有一笔当日仓（平完即回空仓，锁仓态不可达）
        # → latest 就是那笔，也是唯一的平今目标，无需再查。
        return _Action(OrderIntent.CLOSETODAY, net_side,
                       min(policy.lots_per_order, abs(net)),
                       latest, is_exit=True, transition=4)
    # 第 3 列 = CLOSE：反向开仓锁仓（净敞口归零，进入锁仓态）
    return _Action(OrderIntent.OPEN, _opposite(net_side), abs(net),
                   None, is_exit=True, transition=4)
```

**收益**：

| | 重构前 | 重构后 |
|---|---|---|
| 分支形态 | 嵌套特例（CLOSETODAY 藏在一个 `if` 里，末尾直行语句身兼两职） | 按第 3 列**平铺两路**，一眼看全 |
| `Optional` | 有（`today_target`），需 `is not None` 护栏 | **无** |
| 兜底分支 | 有（且误导） | **无** |
| 二次查找 | 遍历 `positions` 过滤 | 复用已有 `latest` |
| 手数口径 | `min(abs(net), today_target.volume)` | `min(lots_per_order, abs(net))`（与 §建议 3 统一口径一致） |

**零行为变更论证**：两态机下 `abs(net) == latest.volume == lots_per_order`，
故 `min(lots_per_order, abs(net)) == min(abs(net), today_target.volume) == N`，两路手数完全相同。

**不变式若被破坏时的降级行为**（理论上不可达）：不再 `AttributeError` 崩溃，
而是对 `latest` 发一笔合法的 `CLOSETODAY`，手数仍被 `min(..., abs(net))` 与
`_pre_trade_check` 双重夹住 —— 比 B 安全，比 A 少一个分支，比 C 少 4 行。

> 该路径不可达 ⇒ 全量测试不变红即为通过；建议补一条注释性断言（"CLOSETODAY 品种运行态仓单数恒为 1"）
> 放进 §10 的守护测试，防止将来有人破坏两态前提。

---

## 10. 受影响测试清单

| 测试文件 | 位置 | 影响 |
|---|---|---|
| `test_p51_closetoday_switch.py` | `:192-193` | 断言 `CU.prefer_closetoday == True` —— **CU 回到 CLOSETODAY 后保持原样，不用改**；但 `prefer_closetoday` 被删后整条断言要换成"表第 3 列 == CLOSETODAY" |
| `test_p51_closetoday_switch.py` | `:279 [4a]` | 「CLOSETODAY + 非 SHFE/INE → `closetoday_not_supported`」→ 判据换源后重写 |
| `test_p50_review_fixes.py` | `:485` | 断言 `supports_closetoday` 是 property → 随属性删除 |
| `test_p9_czce_fak.py` | `:293 [1j]` | `derive_exchange` → **整条删** |
| `test_p42_price_from_quote.py` | `:459-482 [11a~11g2]` | `exchange_mismatch` 对账 → **整节删（8 条）** |
| `test_p33_state_machine_table.py` | `:469` | 转移④ 注释性断言 → 复核 |
| `test_p46_pta_product.py` / `test_p45_metal_products.py` | — | 复核（CU advanced=FOK 不受影响） |
| `test_product_fee_table.py` | — | 费率表对账 → 决策侧不再读费率，复核 |
| 新增 | — | `FAK ⟹ N == 1` 硬断言的守护测试 |
| 新增 | — | 两态前提守护：**CLOSETODAY 品种运行态仓单数恒为 1**（§9.6 不变式） |
| 新增 | — | 转移④ 两路平铺后的手数断言：`min(lots_per_order, abs(net))` 与旧 `min(abs(net), today_target.volume)` 同值 |

---

## 11. 验收口径

1. **全量测试不变红**（除 §10 中明确要删/改的断言，逐条登记）。
2. **`grep -n "CFFEX\|SHFE\|INE\|DCE\|CZCE\|GFEX" Trading/` → 除注释与纯数据外，零判断分支**（`if exchange == ...` 必须为 0 处）。
3. **`FAK ⟹ N == 1`** 构造期硬断言生效（可写一条"改表为 FAK+N=2 启动必失败"的负向测试）。
4. **8 品种 × 3 列与旧派生值逐格一致**（回归脚本对照）。
5. 启动横幅打印表第 3/4/5 列。

---

## 12. 下一步

1. ✅ 注释批 2 → **已做**（13 处，见 §14.3）。
2. ✅ §9 兜底三选项 → **已按 D 实现**（消除 Optional、两路平铺，无兜底分支，§9.6）。
3. ✅ 沙盒实现 → **已做**（`sandbox\chanpy\`，**未直接改 `C:\my_chan_project`**）。
4. ✅ 全量回归 → **52/52 绿**（改前基线 52/52、改后 52/52、解压复验 52/52，三轮一致）。
5. ✅ 按仓库原目录层级打 zip → `deliver\chanpy_exec_policy_2026-09-16.zip`（19 个 `.py`，`.md` 未进包）；已解压到独立临时目录复验（跑测试 + grep 新旧标记）。
6. ⬜ **用户侧**：把 zip 按目录层级解压覆盖到 `C:\my_chan_project`（整文件覆盖语义，无 patch）。
7. ⬜ 出设计文档 HTML 版（可选）。

---

## 13. 坑点（都是真踩过的）

| # | 坑 | 说明 |
|---|---|---|
| 1 | **交易所能力 ≠ 决策判据** | 我第 1 轮建议"能力闸门继续由 exchange 派生"，**被用户推翻**。SHFE 支持平今 ≠ 每个品种都要平今（CU 差点被改错）。**只看第 3 列** |
| 2 | **费率对账是错的** | 我第 2 轮建议"用费率重算 3× 判据 vs 表格第 3 列"对账。TA 费率算平今省 75% 但表填 CLOSE → 必然假警。**已撤回**。费率数据保留给会计侧，决策侧零引用 |
| 3 | **CU 两次翻转** | 第 2 轮用户写 CLOSE → 第 3 轮复算翻回 CLOSETODAY。**以本文档 §3 为准** |
| 4 | **手数三源是真 bug** | 不是"顺手优化"。TA 现在靠"持仓恒 1"侥幸正确，N 一放开就露馅（§5.3） |
| 5 | **`FAK ⟹ N==1` 是表格化新引入的风险** | 现状靠"N 恒为 1"才让 FAK ≡ FOK。表格开放 N 后必须硬约束，否则破坏不变量 4 |
| 6 | **CLOSETODAY ≠ 永发 CLOSETODAY** | 昨仓离场仍走转移⑤ 的 `CLOSE`（平昨）。第 3 列只约束**今仓**。表头要写死，否则后来者会误读 |
| 7 | ~~`Engine.py:1027-1028` 是"两态"的唯一破口~~ ⚠️**表述错误，已修正** | 见第 8 条。那句 `return ... OPEN` **不是破口也不是兜底**，它是 `第 3 列 = CLOSE` 品种的正常出口；真正的"破口"是 `if today_target is not None:` 那 3 行，且它结构性不可达 |
| 8 | **别把 `if/else` 之后的直行语句读成 else 分支** | 我连续 3 轮把 `Engine.py:1027` 当成 `today_target is None` 的降级分支，据此提出 A/B/C 三种"兜底风味"让用户选 —— **伪命题**。读嵌套 `if` 时先确认后续语句是否在 `else` 块内 |

---

## 14. 实现落地记录（2026-09-16 完成）

### 14.1 交付物

| 项 | 值 |
|---|---|
| zip | `C:\Users\river\WorkBuddy\2026-09-16-05-21-13\deliver\chanpy_exec_policy_2026-09-16.zip` |
| 条目 | **19 个 `.py`**（生产 7 + 测试 12），层级 = `Trading/**`（直接覆盖到仓库根） |
| 语义 | **整文件覆盖**，无 patch / diff；`.md`、实验脚本、`__pycache__`、`state.db` 均未进包 |
| 复验 | 解压到独立临时目录 `sandbox\_verify_tmp2\`（原仓库副本 + zip 覆盖）→ 跑测试 **52/52 绿** + grep 新旧标记 |

### 14.2 生产代码（7 个）

| 文件 | 改动 |
|---|---|
| `Trading/Infra/Product.py` | 新增 `CLOSE/CLOSETODAY/FOK/FAK` 常量 + `ExecPolicy`（frozen dataclass，`__post_init__` 三列校验 + `FAK ⟹ N==1` 硬断言）+ `EXEC_POLICY` 8 行表；`Product` 加 `exec_policy` 字段，`_exec_kw(code)` 注入 8 条档案；**删** `supports_closetoday` / `prefer_closetoday` / `prefer_closetoday_at` / `__post_init__` / `LOCK_PATH_MULT` / `_KIND_RATE` / `_KIND_PER_LOT` |
| `Trading/Infra/Instrument.py` | **删** `supports_closetoday`、`derive_exchange`；新增 `exec_policy` property；`effective_order_advanced()` 改读表第 2 列 |
| `Trading/Engine/Engine.py` | `lots_per_order` 改 `@property`（`min(lots_per_signal, 表第 3 列)`）；`_open_volume()` 返回它；**转移④ 两路平铺**（结论 D，无 Optional、无兜底）；**删** `_prefer_closetoday`；`_pre_trade_check` 平今判据改查表第 1 列 |
| `Trading/Broker/SimNow.py` | 删 `derive_exchange` import 与 `exchange_mismatch` 对账告警块 |
| `Trading/Broker/Base.py` | CLOSETODAY 启用判据注释改为「表第 1 列」 |
| `Trading/Infra/Records.py` | 同上；枚举行注释由「仅 SHFE/INE」改为「按表第 1 列」 |
| `Trading/main.py` | `_fee_banner` 改读 `p.exec_policy`（close_mode / 结论 / 报单 / 手数）；删离线 `derive_exchange` 对账 |

### 14.3 测试（12 个）—— 判据换源 + 注释批 2（13 处）

| 文件 | 改动要点 |
|---|---|
| `test_p51_closetoday_switch.py` | **整体重写**为「品种执行策略表」契约：表取值 / `FAK⟹N=1` 硬断言 / 两路平铺（含两态前提）/ 校验链判据 = 表 / 交易所名不参与判断 |
| `test_p9_czce_fak.py` | [1] 段：唯一口径 = 表第 2 列 |
| `test_p42_price_from_quote.py` | 删 `derive_exchange` 整节 + import |
| `test_p46_pta_product.py` | [4] 改表口径；「TA 配错交易所(SHFE) 仍 `_open_volume()=1`」 |
| `test_p50_review_fixes.py` | ctor 补 `exec_policy=`；`supports_closetoday` → `exec_policy` + 负向断言 |
| `test_product_fee_table.py` | [4] 改为「决策侧不再有平今费率派生」；**[6] 整段重写**为表口径（`close_mode` 取值域 / 8 品种逐项 / 无空值 / 旧符号零残留 / `exec_policy is EXEC_POLICY[c]` 同源） |
| `test_p15a_open_lots.py` | 新增 [2b]：`max_volume` 5/20 仍被表压到 2 手（min(表, 风控)） |
| `test_p24 / p33 / p41 / p45` | 注释批 2：把「按交易所分支 / 费率 3× 派生 / `supports_closetoday` 守卫」改写为「按表第 1、2、3 列」（**只改文案，不动期望值**） |

### 14.4 三轮回归（同一口径）

| 轮次 | 范围 | 结果 |
|---|---|---|
| 基线（改动前沙盒） | 52 | 52 / 0 |
| 改动 + 测试改造 + 注释批 2 后 | 52 | 52 / 0 |
| **解压复验**（独立临时目录） | 52 | **52 / 0** |

### 14.5 实现期新踩的坑（补 §13）

| # | 坑 | 说明 |
|---|---|---|
| 9 | **「1 笔挂 N 手」的帽子只属于 OPEN** | 我一开始把 `lots_per_order`（min 表第 3 列）也套到平仓路径 ③④⑤，导致平仓只平 N 手而非全额（p15b [4.5] 跨日仓止损没平、p24 量算错）。**修正**：③④⑤ 手数回到 `min(净敞口/信号手数, 目标手数)`，只有 OPEN 用 `lots_per_order` |
| 10 | **改表后"配错交易所"的期望值必须跟着改** | p46 原期望 TA 配 SHFE 时 `lots_per_signal=2`；现在按表恒 = 1（不看交易所名字）。**这正是本表的目的**，别当回归失败 |
| 11 | **CRLF 与缩进会让整块替换失配** | 测试文件部分为 CRLF、docstring 缩进对不齐 → 改用「读时归一 + 行首空格数不敏感匹配 + 重跑保护（新文案已存在则跳过）」的 patch 脚本，比逐条 Edit 稳 |

## 附录 A：我给出的建议及最终裁定

| # | 建议 | 裁定 |
|---|---|---|
| 1 | 第 3 列改名为「今仓离场 offset」（不是"是否支持平今指令"） | ✅ 采纳（语义碰撞：`CLOSE` 对 CFFEX 是"交易所不支持"，对 SHFE 可能是"我们选择不走"） |
| 2 | 能力闸门仍由 exchange 派生 + 交叉断言 | ❌ **推翻**（用户第 2 轮 ⑴） |
| 3 | 加跨列不变式 `FAK ⟹ N == 1` | ✅ 采纳，**做硬断言**（用户第 2 轮 ⑵⑶） |
| 4 | 顺手统一手数三源 | ✅ 采纳，且升级为"真 bug" |
| 5 | 保留费率 ⇄ 表格对账（WARN） | ❌ **我自己撤回**（坑点 #2） |
| 6 | 落地形态 `EXEC_POLICY` + `_exec_kw` | ✅ 采纳（与 `_fee_kw` 同构，不加 Infra 文件） |
| 7 | exchange 也收进表 | ✅ 采纳（第 2 列，纯注释） |
| 8 | `CLOSETODAY ⟹ exchange ∈ {SHFE,INE}`（I3） | ❌ **删除**（用户第 3 轮 ⑶） |
| 9 | 待定 3 处（derive_exchange / 两处对账） | ❌ **删除**（用户第 4 轮 ⑵） |

## 附录 B：关键代码坐标速查

### B.1 改动后（2026-09-16 实现版 · **以此为准**）

| 位置 | 内容 |
|---|---|
| `Infra/Product.py:164-234` | 品种执行策略表区块：`ExecPolicy`(L200) + `EXEC_POLICY` 8 行(L225)；硬断言在 `__post_init__` |
| `Infra/Product.py:352` | `_exec_kw(code)` —— 表 → 档案注入口（与 `_fee_kw` 同构） |
| `Infra/Instrument.py:332 / 350` | `exec_policy` property / `effective_order_advanced()`（读表第 2 列） |
| `Engine/Engine.py:948 / 964` | `lots_per_order` property（min(风控, 表第 3 列)）/ `_open_volume()` |
| `Engine/Engine.py:1010 / 1021` | `_decide_exit()` / 转移④ 两路平铺（结论 D，**无 Optional、无兜底**） |
| `Engine/Engine.py:1203` | `_pre_trade_check` 的 CLOSETODAY 判据（查表第 1 列） |
| `Test/test_p51_closetoday_switch.py` | 表契约总测试（取值 / 硬断言 / 两路平铺 / 校验链 / 交易所不参与） |

### B.2 改动前（设计期坐标，仅供对照考古）

| 位置 | 内容 |
|---|---|
| `Engine.py:938-946` | 架构约束 A1 / A3 注释 |
| `Engine.py:947-962` | `_open_volume()` |
| `Engine.py:964-995` | `_decide_action()`（转移 ①②③）；`:987-992` = 同构兜底先例 |
| `Engine.py:997-1036` | `_decide_exit()`（转移 ④⑤）；`:1023` = 本文 §9（**结论 D：消除 Optional，两路平铺**） |
| `Engine.py:1038-1063` | `_prefer_closetoday()` |
| `Engine.py:1214-1243` | `_pre_trade_check` 的 CLOSE / CLOSETODAY 校验链 |
| `Engine.py:1561 / 1608` | `_force_exit` / `shutdown_and_lock_all` |
| `Instrument.py:109-110` | FOK/FAK 注释 |
| `Instrument.py:323-341 / 357-380 / 572-584` | `supports_closetoday` / `effective_order_advanced` / `derive_exchange` |
| `Product.py:200-319` | `PRODUCT_PROFILES` 条目 + 费率 + 平今派生 |
| `SimNow.py:797-808 / 962-963 / 1158-1162` | exchange 对账 / 过时的"CLOSETODAY 不可达"注释 / P-A 派生 |
| `main.py:51 / 78-96 / 220-229` | import / 费率横幅 / 对账 |
