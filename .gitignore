*.py[co]
__pycache__
*-checkpoint.ipynb
*.vscode
*.log
*.db
config.yaml
OpenConfig.yaml
gotify_push.py
cos_config.yaml
# 阶段 2：配置中心化（V10 方案 7.3 凭据安全）；P2-2 同花顺工具链迁入 Script/
.env
Script/ths_captured_cookie.txt
Script/.sync_tdx_config
# 运行时缓存（stock_names / stock_pettm_index / stock_float_mc / last_code_freq
# 等联网刷新产生的易变 json，不入库，避免无意义 diff 与合并冲突）
App/*.json
# 门禁运行时产物：Test/run_all.py 每次跑完覆写 Test/report.json（组件结果汇总），
# 与上方同属「易变 json 不入库」——入库会让每次回归都产生无意义 diff 与合并冲突。
# 注意：该文件**已被跟踪**，新增本条目不会自动生效，须先执行
#     git rm --cached Test/report.json
# 并随下次提交落库。
Test/report.json

# ===== Trading 运行产物与本地配置 =====
# 回放/实盘的输出目录（events.jsonl、summary 等）与 sqlite 状态目录
Trading/State/
# 本地个性化配置已改为：Trading/Config.py（入库默认值）+ 环境变量/仓库根 .env（不入库）
# SQLite WAL/SHM 附加文件（与上方 *.db 配套；*.db 已 ignore 但 WAL/SHM 需单独忽略）
*.db-wal
*.db-shm
